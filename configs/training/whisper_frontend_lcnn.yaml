data:
  seed: 42

checkpoint:
  path: ""

model:
  name: "whisper_frontend_lcnn"
  parameters:
    freeze_encoder: True
    input_channels: 2
    frontend_algorithm: ["lfcc"]
  optimizer:
    lr: 0.0001
    weight_decay: 0.0001

